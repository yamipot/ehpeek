{
  "name": "ehpeek-workspace",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @ehpeek/reader build && pnpm --filter ehpeek build && pnpm --filter @ehpeek/reader build:example",
    "check": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint . --max-warnings=0",
    "example": "pnpm --filter @ehpeek/reader example",
    "test": "pnpm --filter @ehpeek/reader build && pnpm --filter @ehpeek/reader check:consumer && node --test packages/reader/tests/*.test.mjs"
  },
  "devDependencies": {
    "@adobe/spectrum-tokens": "14.15.0",
    "@eslint/js": "^9.39.5",
    "esbuild": "^0.28.1",
    "esbuild-plugin-solid": "^0.6.0",
    "eslint": "^9.39.5",
    "eslint-plugin-solid": "^0.14.5",
    "globals": "^17.7.0",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.64.0",
    "unocss": "^66.7.5"
  }
}
